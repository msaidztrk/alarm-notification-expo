// UI Components - Using React Native Animated API (no Reanimated required)
// These components provide smooth animations without native module dependencies

export { AlarmCard } from '../AlarmCard';
export { default as NotificationCard } from '../NotificationCard';

// Note: GlassCard, AnimatedSwitch, PulseButton, and IconButton are now
// integrated directly into AlarmCard.tsx for simplicity.
// Import from AlarmCard if you need those components.
